[package]
name = "pfe_core"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy_ecs = "0.17.3"
polars = { version = "0.52.0", features = [
    "default",
    "lazy",        # Enables the LazyFrame API (essential for query optimization)
    "sql",
]}
rusqlite = "0.37.0"
rand = "0.9.2"
fastrand = "2.3.0"