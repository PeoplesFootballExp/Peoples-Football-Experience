#[derive(Component)]
pub struct Position {
    pub x: i32,
    pub y: i32,
    pub z: i32,
}

pub struct Velocity {
    pub x: i32,
    pub y: i32,
    pub z: i32,
}

pub struct Acceleration {
    pub x: i32,
    pub y: i32,
    pub z: i32,
}