#[derive(Component)]
pub struct PlayerID {
    pub id: u32,
}

#[derive(Component)]
pub struct TerritoryID {
    pub id: u32,
}

#[derive(Component)]
pub struct ConfederationID {
    pub id: u32,
}

#[derive(Component)]
pub struct ManagerID {
    pub id: u32,
}

#[derive(Component)]
pub struct StadiumID {
    pub id: u32,
}